import react from 'react';

const SignUp = () => {
  return (
    <div>
      <h1>
        You don't need to sign up because I like you, so you're already a member.
      </h1>
    </div>
  );
};

export default SignUp;